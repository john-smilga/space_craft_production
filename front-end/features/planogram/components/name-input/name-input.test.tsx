import { describe, it, expect } from 'vitest';
import { NameInput } from './name-input';

describe('NameInput', () => {
  it.skip('should render correctly', () => {
    // TODO: Add actual test implementation with required props (value, onChange)
    expect(NameInput).toBeDefined();
  });
});

